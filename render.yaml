services:
  - type: web
    name: chatbot
    env: python
    region: singapore
    branch: main

    preInstall: |
      rm -f pyproject.toml
      rm -f poetry.lock

    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

    envVars:
      PYTHON_VERSION: 3.11.0
